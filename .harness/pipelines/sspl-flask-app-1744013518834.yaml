pipeline:
  identifier: Build_hdas2_sspl_flask_app_1744013522112
  name: Build sspl-flask-app
  orgIdentifier: default
  projectIdentifier: gitopsproject
  properties:
    ci:
      codebase:
        build: <+input>
        connectorRef: account.Github
        repoName: hdas2/sspl-flask-app
  stages:
    - stage:
        identifier: build
        name: build
        spec:
          caching:
            enabled: true
          cloneCodebase: true
          execution:
            steps:
              - step:
                  identifier: echo
                  name: echo
                  spec:
                    command: echo hello world
                  timeout: ""
                  type: Run
          platform:
            arch: Amd64
            os: Linux
          runtime:
            spec: {}
            type: Docker
        type: CI
